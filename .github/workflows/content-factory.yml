# Content Factory - Automated Video Generation & Upload
# Generates ambient videos and publishes to YouTube on schedule

name: Content Factory

on:
  # Run daily at 2 AM UTC
  schedule:
    - cron: '0 2 * * *'
  
  # Manual trigger with options
  workflow_dispatch:
    inputs:
      moods:
        description: 'Moods: rain_sleep, fireplace, ocean_waves, lofi_study, piano_relax, forest_morning, deep_focus, sleep, trance (comma-separated or "all")'
        required: false
        default: 'rain_sleep,fireplace,ocean_waves'
      durations:
        description: 'Durations: 30s, 10min, 1h, 3h, 8h (longer = more watch time)'
        required: false
        default: '3h'
      dual:
        description: 'Generate BOTH ambience-only and melody versions (2x videos)'
        required: false
        type: boolean
        default: true
      upload:
        description: 'Upload to YouTube after generation'
        required: false
        type: boolean
        default: true

env:
  PYTHON_VERSION: '3.11'

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 360  # 6 hours max for long videos
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
      
      - name: Install FFmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install google-auth-oauthlib google-api-python-client
      
      - name: Generate videos
        run: |
          DUAL_FLAG=""
          if [ "${{ github.event.inputs.dual || 'true' }}" = "true" ]; then
            DUAL_FLAG="--dual"
          fi
          python batch_generate.py \
            --moods "${{ github.event.inputs.moods || 'rain_sleep,fireplace,ocean_waves' }}" \
            --durations "${{ github.event.inputs.durations || '3h' }}" \
            $DUAL_FLAG \
            --output ./generated
      
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: generated-videos
          path: ./generated/
          retention-days: 7
      
      - name: Restore YouTube credentials
        if: ${{ github.event.inputs.upload == 'true' || github.event_name == 'schedule' }}
        run: |
          echo "${{ secrets.YOUTUBE_TOKEN_PICKLE }}" | base64 -d > youtube_token.pickle
          echo '${{ secrets.YOUTUBE_CLIENT_SECRETS }}' > client_secrets.json

      - name: Upload to YouTube
        if: ${{ github.event.inputs.upload == 'true' || github.event_name == 'schedule' }}
        run: |
          python youtube_upload.py --batch ./generated
      
      - name: Summary
        run: |
          echo "## Content Factory Run Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Generated Videos:" >> $GITHUB_STEP_SUMMARY
          cat ./generated/manifest.json | python -c "
          import sys, json
          data = json.load(sys.stdin)
          dual = data.get('dual_mode', False)
          print(f\"- Mode: {'DUAL (Ambience + Melody)' if dual else 'Single'}\")
          print(f\"- Jobs: {data['total']}\")
          print(f\"- Success: {data['success']}\")
          print(f\"- Total Videos: {data.get('total_videos', data['success'])}\")
          for v in data.get('videos', []):
              r = v.get('result', {})
              print(f\"  - {r.get('video_title', 'Unknown')}\")
          "

  # Optional: Notify on completion
  notify:
    needs: generate
    runs-on: ubuntu-latest
    if: always()
    
    steps:
      - name: Send notification
        run: |
          echo "Content Factory completed with status: ${{ needs.generate.result }}"
          # Add Discord/Slack webhook here if desired

